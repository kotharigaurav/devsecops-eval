# k8s/secret.yaml
# TEMPLATE only: do NOT commit real credentials. Replace values locally or create the
# secret at deploy time via CI using GitHub Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: default
type: Opaque
stringData:
  DB_USER: "<REPLACE_WITH_DB_USER>"
  DB_PASS: "<REPLACE_WITH_DB_PASS>"
